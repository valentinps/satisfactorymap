[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "sav_parse_rs"
version = "0.1.0"
description = "Rust rewrite of the Satisfactory save parser (drop-in backend for sav_parse)"
requires-python = ">=3.12"

[tool.maturin]
module-name = "sav_parse_rs"
