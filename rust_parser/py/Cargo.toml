[package]
name = "sav_parse_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "sav_parse_rs"
crate-type = ["cdylib"]

[dependencies]
sav_core = { path = "../core", features = ["pyo3-errors", "parallel"] }
pyo3 = { version = "0.23", features = ["abi3-py312", "extension-module"] }
