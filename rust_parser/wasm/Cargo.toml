[package]
name = "sav_wasm"
version = "0.1.0"
edition = "2021"

[lib]
name = "sav_wasm"
crate-type = ["cdylib"]

[dependencies]
# No "parallel" (single-threaded wasm MVP) and no "pyo3-errors".
sav_core = { path = "../core" }
wasm-bindgen = "0.2"
js-sys = "0.3"
console_error_panic_hook = "0.1"
